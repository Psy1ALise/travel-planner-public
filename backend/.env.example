# Database Configuration
DATABASE_NAME=your_database_name
CLOUD_SQL_CONNECTION_NAME=your_cloud_sql_connection_name
DATABASE_USERNAME=your_database_username
DATABASE_PASSWORD=your_database_password

# JWT Configuration
JWT_SECRET_KEY=your_jwt_secret_key

# Google Cloud Storage
GCS_BUCKET=your_gcs_bucket_name

# Google Cloud Credentials
# For local development:
# 1. Get credentials.json from Google Cloud Console
# 2. Place it in backend/src/main/resources/
# 3. Set the path below to point to your credentials file
GOOGLE_APPLICATION_CREDENTIALS=src/main/resources/credentials.json

# For production:
# 1. Use Google Cloud Run's built-in service account
# 2. Or set up Workload Identity Federation
# 3. The path should be: /app/credentials.json as specified in Dockerfile

# CORS Configuration
FIREBASE_APP_URL=https://your-app-id.web.app
FIREBASE_APP_DOMAIN=https://your-app-id.firebaseapp.com
BACKEND_API_URL=https://your-backend-url.run.app 